/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'a5vbqirvma6osqen.public.blob.vercel-storage.com',
          'i.ibb.co',
          'unavatar.io',
          'img.daisyui.com']
      }
};

export default nextConfig;
